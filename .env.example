SUPABASE_URL=
# Prefer service role for server-side functions. Keep it secret and never expose in browser JS.
SUPABASE_SERVICE_ROLE_KEY=
# If you only have anon key, you can use it, but your Supabase RLS must allow these operations.
SUPABASE_ANON_KEY=

JWT_SECRET=change-me
COOKIE_NAME=armchair_session
COOKIE_MAX_AGE_SECONDS=28800

